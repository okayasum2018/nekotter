
    <div class="contents">
      <% @tweets.each do |tweet|%>
  　　　 <%= render partial: "tweet", locals: { tweet: tweet } %>
        <% end %>
    <div class="contents2">
      <%= paginate(@tweets) %>
   </div>

 </div>
